
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Local secrets — only .env.example is committed.
# Real .env lives on the VM at /opt/pss-supabase-host/.env (mode 600).
.env
.env.local
*.env.local
